machine:
  services:
    - postgresql

dependencies:
  override:
    - nvm install 4.2.1
    - nvm alias default 4.2.1
    - npm install -g npm@2.8.3
    - npm install

database:
  pre:
    - createuser astral-test
    - createdb astral-test
    - npm run migrate-test
